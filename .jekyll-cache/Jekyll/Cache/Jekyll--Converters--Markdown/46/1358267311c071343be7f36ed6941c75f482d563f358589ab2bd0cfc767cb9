I""<p>Expressões são construídas através de operações e operandos. Os operadores de uma expressão indicam quais operações podem ser aplicadas nos operandos.</p>

<p>Operadores são construções de linguagem que carregam algum tipo de comportamento. Há basicamente três grupos de operadores:</p>

<ul>
  <li>Operadores aritméticos (como o <code class="highlighter-rouge">+</code> na expressão <code class="highlighter-rouge">1 + 3</code>),</li>
  <li>Operadores de comparação (como o <code class="highlighter-rouge">&gt;</code> na expressão <code class="highlighter-rouge">1 &gt; 3</code>), e</li>
  <li>Operadores lógicos (como o <code class="highlighter-rouge">&amp;&amp;</code> na expressão <code class="highlighter-rouge">1 &amp;&amp; 3</code>).</li>
</ul>

<p>Os operadores de uma linguagem de programação como Ruby são fortemente inspirados dos operadores matemáticos.</p>

<p>é um símbolo especial que indica um</p>

<p>Existe também a ordem de avaliação dos operadores; há operadores que precisam ser executados antes de outros. A ordem de avaliação é determinada pela <em>precedência</em> dos operadores.</p>

<p>Operadores aritimeticos são métodos</p>

<p>https://stackoverflow.com/questions/39311429/multiplying-string-by-integer-vs-integer-by-string-in-ruby/39311454</p>
:ET